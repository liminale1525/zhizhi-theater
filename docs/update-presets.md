# 作者预设更新说明

目前最稳的方式：

1. 编辑 `index.html` 里的 `defaultState`。
2. 同步更新 `data/presets.json` 作为外部备份。
3. 提交到 GitHub。
4. GitHub Pages 自动发布新版页面。

注意：

- 已经使用过网页的用户，本地浏览器里有自己的 `localStorage` 数据。
- 作者更新默认预设后，不会强行覆盖这些用户已有数据。
- 如果需要让老用户导入新版预设，建议之后增加一个“导入作者预设”按钮，而不是自动覆盖。
